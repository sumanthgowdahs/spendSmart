import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function SpendLint() {
  return (
    <View>
    <Text>Recent Spent</Text>
    <ScrollView>
        
    </ScrollView>
    </View>
  )
}

const styles = StyleSheet.create({})